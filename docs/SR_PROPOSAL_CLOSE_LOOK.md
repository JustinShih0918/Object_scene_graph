# Proposal: a close look at a surface, before the search moves on and before absence is declared

Written 2026-09-09 against the tight-ring run (`outputs/osg_dualmap_tightring`,
in-anchor 24/54, cross-anchor 15/53 at DualMap's 1 m rule). The evidence it
rests on is the "What the old headline was made of" section of
`DUALMAP_OFFICIAL_RERUN.md`; the tables are regenerated by
`scripts/analyze_headline_anatomy.py`. No agent code is changed by this document.

## The two failures it targets

Both survive every change of benchmark and rule made so far, and both are
about *where the camera is when it looks*, not about what the detector can do
once it is there.

**Cross-anchor: the object's own surface is never a goal.** In all 32
cross-anchor failures without a detection, the mapped container the object sat
on -- within 1.5 m of it every time, a bed in half of them -- was selected by
the search 0 times and arrived at 0 times, while those episodes made 4-21
surface selections and 150-2300 glance updates each. Twelve of the 32 did have
the object in frame, at 2-4 m, during `goto_frontier`, where the detector's
in-situ recall is 0.28; nine of the thirteen shared-target "seen, never named"
failures are that, not the detector. The chain is:

* `container_prior` (`src/osg/exploration/search_belief.py`) is affinity times
  proximity, and proximity is `exp(-d / search_proximity_len_m)` from the
  *stale* position with `search_proximity_len_m = 1.0`. A surface 5.6 m from
  where the object used to be scores e^-5.6, 0.4% of the peak, before the x4
  same-room bonus. `osg-cross-anchor-is-proximity-bound` measured this before;
  the proximity-floor arms F3 / X4 / F5 moved coverage and converted nothing,
  because the sightings they bought were far ones.
* `glance` (`src/osg/exploration/strategy.py`) retires any container in view
  out to `search_glance_range_m = 4.0` by `search_glance_detect_prob = 0.35`
  per keyframe, floored at 0.2. A surface passed at 3.5 m is credited as 80%
  searched at a range where the detector would have missed the object seven
  times in ten.
* Candidate detection does run during `goto_frontier`
  (`nav_agent.py`, the `candidates.check` call), so those twelve sightings had
  the detector on them. Recall at that range is what it is.

**In-anchor: the agent arrives at the right place and leaves.** The first
commit is already within 1 m of the object's new position in 23 of 44
in-anchor episodes with a commit (29 of 48 in condition N). Five of those ended
more than 3 m away, three after `absence_abandon`; ten more were named or
admitted and never committed. On a silent arrival the approach turns to face the
*track centre* (`approach.py`, `scan_at_viewpoint`), then `_absence_at_arrival`
hands the frame to `AbsenceSensor.observe` with `abandon_below_p = 1.0`, and
the agent goes back to EXPLORE. With the tight ring at 0.35-0.65 m and a
79-degree horizontal field, the frame is 0.3-0.5 m wide at the object, and the
object moved a median 0.7 m: the frame the absence decision is taken on usually
cannot contain it.

## One primitive, used twice

`CloseLook(container_id)`: plan to a facing pose on a **1.5 m** ring around the
container, turn until the container is inside 15 degrees, hold for about three
keyframes with the detector on, and only then let anything be concluded --
the surface's `searched` factor for the search, or the absence reading for a
committed track.

Why 1.5 m: it is where in-situ recall peaks (0.58 at 1.5-2.5 m against 0.50
inside 1.5 m and 0.28 beyond 2.5 m, `PERCEPTION_INVESTIGATION.md`), and the
frame is 2.4 m wide there, so a 0.7 m displacement stays in it.

Why it costs nothing at runtime: it is a navigation policy, not a model. No new
network, no per-frame work; the price is steps, about ten per look, which the
A/B measures.

Everything it needs exists:

* the ring planner, `ViewpointPlanner.approach_viewpoint`, with the extent
  offset from the tight-ring change so a 1.5 m ring around a bed lands outside
  the bed;
* `face_surface` in `strategy.py`, which already turns toward a selected
  surface within `search_arrival_m` and counts `surface_face_turns`;
* the container layer, `floor -> room -> container -> object`, which gives a
  committed track its `container_id`;
* `Approach.retarget`, which re-aims a running approach when the track moves
  (`agent.approach_retarget_m`, currently 0.0).

### 1. Cross-anchor: the opportunistic close look

In `goto_frontier` and `explore`, when an affording container (the same
`affords()` gate the prior uses) is within 2.5 m, inside the frame, on this
floor, and has not had a close look this episode, detour to `CloseLook` and
then resume the frontier. Cap at `close_look_max_per_episode` (start at 8) and
skip surfaces already inspected by arrival. Passive glances beyond 2.5 m stop
retiring belief (`search_glance_range_m: 2.5`); inside it they are replaced by
the look. This is the active version of what the `glance` docstring already
promises -- "most of those surfaces are simply in view along the way; looking
counts" -- with "looking" made to mean something the detector can use.

The 12 exposure episodes are the population it targets. At the measured
close-range recall of the shared targets it converts perhaps six; the 12
never-in-view episodes convert only where the frontier path happens to pass
their surface, and the 8 detector-wall ones (7 scissors) not at all.

### 2. In-anchor: the close look before absence

On a silent `path_consumed` or `deadline` arrival at a committed track that
was never seen live during this approach (`last_good_xy is None` -- a
prior-map track), run `CloseLook` on the track's container **before**
`_absence_at_arrival`. If the detector names the target during the look, re-aim
through `retarget` (`agent.approach_retarget_m: 0.5`) and continue to the tight
ring; if it does not, take the absence reading from the look pose, which is a
frame that could have contained the object, and abandon as now.

This does not touch `abandon_below_p` or the reading arithmetic. It changes
which frame the reading is taken on.

## Instrumentation first

Nothing above is enabled before its counter exists (`osg-verify-the-knob-moved`:
two of five arms once returned confident nulls while testing nothing).

Ground-truth side, in `src/osg/eval/instruments.py` and the episode record:
`gt_true_container_id` -- the nearest affording container to the target's new
position -- and, per episode, whether that container was glanced, close-looked,
selected and arrived at, with the minimum range and off-axis of any look at it.
Agent side: `close_look_started`, `close_look_detected`, `close_look_abandoned`,
`close_look_steps`, and for the in-anchor path `absence_after_look`.

The offline gate is the last table of `HEADLINE_ANATOMY.md`: own-surface
close-looked must move from 0/32 to well above zero, and the close-centred
keyframe count of the exposure episodes must rise, before any SR is read.

## The A/B

Paired on trial id, as `scripts/compare_tightring.py` does, over the 107
dynamic trials, three scenes in parallel with the invocation in
`DUALMAP_OFFICIAL_RERUN.md`. Four arms: the tight-ring baseline; + the in-anchor
look; + the opportunistic look; both. Report, in this order:

1. the mechanism counters above, per arm;
2. the funnel from `analyze_dynamic_failures.py`, so a gain is seen to come
   from the bucket it was aimed at;
3. cost: median steps and trials at budget, from the existing cost table --
   a look is ten steps and the cross-anchor budget is already exhausted in 27
   of 53 trials;
4. SR with the sign test, stated with its p-value.

## What it can buy, and what it cannot

From the funnel: in-anchor has 5 arrive-and-leave episodes and 10 named or
admitted but never committed, so the ceiling is about +28 points and a realistic
gain +10-15; cross-anchor has 12 exposure episodes at roughly 0.5 close-range
recall, about six conversions or +11 points, plus whatever the detour reaches of
the 12 never-in-view. The 16 detector-wall episodes stay where they are and
belong to the paused appearance re-identification plan.

107 trials cannot make a 10-point change significant; `AB_RESULTS.md` says so
and the tight-ring arm proved it again. The result of this work is the counters
moving and the funnel emptying from the right buckets. If they do, SR follows,
and if they do not, SR is not to be read.

## Implementation (2026-09-09)

`src/osg/agent/close_look.py`, `CloseLookPolicy`, a state of its own
(`State.CLOSE_LOOK`) with the two entry points above behind
`agent.close_look_before_absence` and `agent.close_look_opportunistic`, both
off by default. The hooks are one line each: `ApproachPolicy.step` calls
`before_absence` ahead of `_absence_at_arrival` on the `deadline` and
`path_consumed` endings, and `NavAgent._act_inner` calls
`maybe_opportunistic` on each keyframe after the glance. A commit that
pre-empts a look is recorded as the look succeeding. The look drives with the
approach's own `follow_to`, plans its pose with `ViewpointPlanner`'s new
per-call `radii`, and hands a silent look to `ExplorationStrategy.close_looked`,
which retires the surface as an arrival would and marks it inspected so the
opportunistic trigger never spends a second detour on it.

Telemetry, per episode: `close_look_log` (one entry per look: why, where,
how many steps, whether the detector answered), the `close_look_*` counters in
`agent_stats`, and `glance_ranges`, the closest range each container was ever
glanced from, which is what lets a passing glance at 3.5 m be told apart from
one at 1.2 m offline. `scripts/analyze_headline_anatomy.py` reads both into
the own-surface table; `scripts/compare_close_look.py` is the four-arm report.

**Measured cost on the three-trial smoke of the first cut**: a look was 15-49
steps, and eight of them took 218 of a 481-step cross-anchor episode. The
shipped defaults bound a look at about 15 steps (25 driving, six facing turns,
a two-step hold) and an episode at six looks. The in-anchor smoke trial did
what the design says: silent arrival, look, detection at 1.55 m, re-aim.

Presets: `dualmap_protocol_osg_look_inanchor`, `_look_opportunistic`,
`_look_both`, all on top of `dualmap_protocol_osg_tightring`; the batch is
`scripts/run_close_look_ab.sh`.

## Result (2026-09-09): one half works, the other half was aimed at the wrong thing

Four arms, 107 dynamic trials each, paired on trial id against the tight-ring
baseline; `scripts/compare_close_look.py` writes
`outputs/osg_closelook/CLOSE_LOOK_AB.md`. Every knob moved, so the nulls below
are nulls and not untested arms.

| arm | looks | detected during a look | re-approached | steps spent looking | median steps | at budget |
|---|---:|---:|---:|---:|---:|---:|
| baseline | 0 | 0 | 0 | 0 | 421 | 47 |
| before absence | 60 | 10 | 10 | 1136 | **366** | 46 |
| opportunistic | 424 | 50 | 0 | 8010 | 402 | 50 |
| both | 427 | 56 | 12 | 7970 | 386 | 47 |

| split | baseline | before absence | opportunistic | both |
|---|---:|---:|---:|---:|
| in-anchor SR | 24/54 | 25/54 (+2 / -1) | 23/54 (+2 / -3) | 25/54 (+4 / -3) |
| cross-anchor SR | 15/53 | 16/53 (+3 / -2) | 13/53 (+1 / -3) | 13/53 (+3 / -5) |

No arm is significant (sign test p >= 0.6 throughout), as expected at n = 107.
The mechanism tables are the result.

**The look before absence is a small, cheap positive and stays.** Sixty looks
cost 1136 steps in total, ten of them re-detected the target and re-aimed, and
the median episode got *shorter* (421 to 366 steps) because a re-detection ends
an episode that used to run to the budget: `0117 pitcher` went from 308 steps
and 1.36 m to 68 steps and 0.97 m; `0118 plate` from 11.36 m to 1.66 m; `0118
cracker box` from 8.41 m to 2.58 m. "Seen, never named" fell 10 to 8 in-anchor
and 20 to 16 cross-anchor. What it did not do is empty the arrive-and-leave
bucket: of the 23 in-anchor episodes whose first commit was already within 1 m
of the object, 5 still ended more than 3 m away, in every arm. The look gives
the detector its chance; on those five the detector still said nothing.

**The opportunistic look is a null with a cost, and it is retired.** It fired
424 times, hit its six-look cap in 66 of 107 episodes by a median step 155, and
spent 8010 steps -- 15% of the whole batch's budget -- looking at beds, desks
and cabinets around the *stale* position and along the frontier route. On the
32 cross-anchor failures the proposal aimed it at, the object's own surface
was close-looked in **one**. The reason is in the record it was built to
write: in 28 of those 32 episodes the true surface was never within 4 m of a
single glance. The agent never went near it. Never-in-view rose (12 to 19
cross-anchor, 6 to 10 in-anchor) because the steps the looks consumed were the
steps that used to reach a frontier.

So the diagnosis "exposure: the object crossed the frame at 2-4 m" was right
about the frame and wrong about the remedy. Those sightings were of the
*object* at the edge of a room the agent was leaving, not of a surface it was
passing. Looking harder along the route the search already takes cannot
convert them, because the route is the failure: `container_prior`'s proximity
term, `exp(-d / 1.0)` from the stale position, keeps the true surface out of
the search order for the whole episode (memory:
`osg-cross-anchor-is-proximity-bound`), and the frontier weight of 0.3 keeps
the agent inspecting the stale room. The cross-anchor lever is where the
search *goes*, not how it looks when it gets there.

**Where this leaves the presets.** `close_look_before_absence` is worth carrying
into the best-known configuration; `close_look_opportunistic` and the 2.5 m
glance range are not, and the flags stay off by default. The next cross-anchor
experiment is a search-order one: a proximity length or floor that lets a
surface 5 m from the stale position be selected within the first hundred
steps, measured first by "own surface selected / arrived" in the anatomy table
and only then by SR.

## Iterating faster: the hard subset

A full arm is 107 dynamic trials and about 4.8 hours of process time, at
0.44 s per step. The per-step cost is structural -- nearly every step is a
keyframe, and the object layer (146 ms), scene graph (80-230 ms) and detector
(100 ms) run on each -- so the lever is not the step but which trials run. 68%
of a batch's wall clock is the 46 failures that run to the 500-step budget,
and those are the only trials a search-order or approach change can move.

`scripts/make_hard_subset.py` picks, from a reference run, the cross-anchor
trials where the target was never named, the cross-anchor trials named and
still failed, and the in-anchor trials whose first commit was already within
1 m of the object and which still failed -- and drops scissors and mug, which
the detector cannot name in situ and no search change converts. From the
look-before-absence run that is **33 trials, 1.9 hours of process time**, in
`data/splits/dualmap_hard.json`, with each trial's reference outcome recorded.

`scripts/run_close_look_ab.sh` takes `TRIAL_SET` (the subset), `SHARDS_PER_SCENE`
(split a scene's trials across processes; the subset is lopsided, 16 on 00848
against 6 on 00829) and `MAX_STEPS` (a shorter budget for mechanism-only
iteration; the search-order gate is read in the first ~150 steps, and a
300-step cap forfeits the 6-7 of 107 successes that land after step 300):

    TRIAL_SET=data/splits/dualmap_hard.json SHARDS_PER_SCENE=2 MAX_PARALLEL=6 \
      MAX_STEPS=300 ARMS=drop OUT_ROOT=outputs/osg_hard scripts/run_close_look_ab.sh
    python scripts/compare_close_look.py --arm base=outputs/osg_closelook/inanchor \
      --arm drop=outputs/osg_hard/drop

One arm on the subset, six processes, is about twenty minutes against three
and a half hours for a full three-arm batch. The report pairs on the trial ids
both runs share, so a subset run reads against any full run.

Two things a subset result is not. It is selected on a reference run's
*outcome*, so it selects on noise as well as difficulty -- a trial that failed
once by chance is in, one that scored once by chance is out -- and a gain on
it is a gain on trials chosen for having failed. And a capped budget changes
what "failed" means. Both are for deciding which arm deserves the full 107,
which remains the number reported.
