# @package _global_
#
# `_sensor_v4_fuse` with the commit bar raised to 0.30. On the observation
# runs (docs/PROPOSAL_FUSION.md) that takes phantom episodes 5 -> 1 of 37 and
# true perception tracks 8 -> 3 of 11; on the full 107 at 0.28 the three
# phantom losses all sat below 0.30 and the scissors/mug/banana true tracks
# above it. First read: the eight trials that changed at 0.28.

defaults:
  - dualmap_protocol_osg_sensor_v4_fuse

region_proposal:
  commit_tau: 0.30
